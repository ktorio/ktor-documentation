# HTTP APIs

A sample Ktor project showing how to create [HTTP APIs](https://ktor.io/docs/creating-http-apis.html) using Ktor.

## Running

To run a sample, execute the following command in a repository's root directory:
```bash
./gradlew :tutorial-http-api:run
```

Then, you can test HTTP endpoints as described here: [Manually testing HTTP endpoints](https://ktor.io/docs/creating-http-apis.html#manually-testing-http-endpoints).
